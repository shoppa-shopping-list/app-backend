{
  "name": "shoppa-telegram-relay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "login": "npx wrangler login",
    "tail": "npx wrangler tail"
  }
}
